export const OrderItemCard = ({ items }) => {
  //console.log("Thong tin shop va items tương ứng", items);
  const card = "glass rounded-3xl border shadow-md p-2 md:p-4  ";

  return <div className={`${card}`}>đây là thông tin cần thiết</div>;
};
