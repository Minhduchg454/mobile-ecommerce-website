import { CouponManage } from "../../features";
import { useSelector } from "react-redux";

export const CouponManagePage = () => {
  return (
    <div className="animate-fadeIn">
      <CouponManage createdById="" />
    </div>
  );
};
