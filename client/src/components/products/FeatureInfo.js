import React from "react";
import { infoCards } from "../../ultils/contants";
import { FeatureInfoSlider } from "../../components";

const FeatureInfo = () => {
  return <FeatureInfoSlider data={infoCards} />;
};

export default FeatureInfo;
